import { NFTMintParams, RequiredParam } from "../../types";
import { Erc721, NFTDrop, QueryAllParams, SmartContract, ValidContractInstance } from "@thirdweb-dev/sdk";
/**
 @internal
 */
export declare function detectErc721Instance(contract: RequiredParam<ValidContractInstance | SmartContract>): Erc721<any> | undefined;
/** **********************/
/**     READ  HOOKS     **/
/** **********************/
/**
 * Use this to get a list of NFT tokens of your ERC721 contract.
 *
 * @example
 * ```javascript
 * const { data: nfts, isLoading, error } = useNFTs(<YourERC721ContractInstance>, { start: 0, count: 100 });
 * ```
 *
 * @param contract - an instace of a contract that extends the Erc721 spec (nft collection, nft drop, custom contract that follows the Erc721 spec)
 * @param queryParams - query params to pass to the query for the sake of pagination
 * @returns a response object that includes an array of NFTs
 * @beta
 */
export declare function useNFTs(contract: RequiredParam<Erc721>, queryParams?: QueryAllParams): import("react-query").UseQueryResult<import("@thirdweb-dev/sdk").NFTMetadataOwner[], unknown>;
/**
 * Use this to get a the total (minted) supply of your ERC721 contract.
 *
 * @example
 * ```javascript
 * const { data: totalSupply, isLoading, error } = useNFTSupply(<YourERC721ContractInstance>);
 * ```
 *
 * @param contract - an instace of a contract that extends the Erc721 spec (nft collection, nft drop, custom contract that follows the Erc721 spec)
 * @returns a response object that incudes the total minted supply
 * @beta
 */
export declare function useNFTSupply(contract: RequiredParam<Erc721>): import("react-query").UseQueryResult<import("ethers").BigNumber, unknown>;
/** **********************/
/**  READ HOOKS (drop)  **/
/** **********************/
/**
 * Use this to get a list of *unclaimed* NFT tokens of your ERC721 Drop contract.
 *
 * @example
 * ```javascript
 * const { data: unclaimedNfts, isLoading, error } = useUnclaimedNFTs(<YourERC721DropContractInstance>, { start: 0, count: 100 });
 * ```
 *
 * @param contract - an instace of a contract that extends the Erc721 spec (nft drop, custom contract that follows the Erc721 & drop spec)
 * @param queryParams - query params to pass to the query for the sake of pagination
 * @returns a response object that includes an array of NFTs that are unclaimed
 * @beta
 */
export declare function useUnclaimedNFTs(contract: RequiredParam<NFTDrop>, queryParams?: QueryAllParams): import("react-query").UseQueryResult<{
    [x: string]: import("@thirdweb-dev/sdk").Json;
    name?: string | undefined;
    description?: string | undefined;
    image?: string | undefined;
    external_url?: string | undefined;
    animation_url?: string | undefined;
    uri: string;
    id: import("ethers").BigNumber;
}[], unknown>;
/**
 * Use this to get a list of *claimed* (minted) NFT tokens of your ERC721 Drop contract.
 *
 * @remarks Equivalent to using {@link useNFTs}.
 *
 * @example
 * ```javascript
 * const { data: claimedNFTs, isLoading, error } = useClaimedNFTs(<YourERC721DropContractInstance>, { start: 0, count: 100 });
 * ```
 *
 * @param contract - an instace of a contract that extends the Erc721 spec (nft drop, custom contract that follows the Erc721 & drop spec)
 * @param queryParams - query params to pass to the query for the sake of pagination
 * @returns a response object that includes an array of NFTs that are claimed
 * @beta
 */
export declare function useClaimedNFTs(contract: RequiredParam<NFTDrop>, queryParams?: QueryAllParams): import("react-query").UseQueryResult<import("@thirdweb-dev/sdk").NFTMetadataOwner[], unknown>;
/**
 *
 * @param contract - an instace of a contract that extends the Erc721 spec (nft drop, custom contract that follows the Erc721 & drop spec)
 * @returns a response object that includes the number of NFTs that are unclaimed
 */
export declare function useUnclaimedNftSupply(contract: RequiredParam<NFTDrop>): import("react-query").UseQueryResult<import("ethers").BigNumber, unknown>;
/**
 *
 * @param contract - an instace of a contract that extends the Erc721 spec (nft drop, custom contract that follows the Erc721 & drop spec)
 * @returns a response object that includes the number of NFTs that are claimed
 */
export declare function useClaimedNftSupply(contract: RequiredParam<NFTDrop>): import("react-query").UseQueryResult<import("ethers").BigNumber, unknown>;
/** **********************/
/**     WRITE HOOKS     **/
/** **********************/
/**
 * Use this to mint a new NFT on your ERC721 contract
 *
 * @example
 * ```jsx
 * const Component = () => {
 *   const {
 *     mutate: mintNft,
 *     isLoading,
 *     error,
 *   } = useMintNFT(">>YourERC721ContractInstance<<");
 *
 *   if (error) {
 *     console.error("failed to mint nft", error);
 *   }
 *
 *   return (
 *     <button
 *       disabled={isLoading}
 *       onClick={() => mintNft({ name: "My awesome NFT!" })}
 *     >
 *       Mint!
 *     </button>
 *   );
 * };
 * ```
 *
 * @param contract - an instace of a contract that extends the Erc721 spec (nft collection, nft drop, custom contract that follows the Erc721 spec)
 * @returns a mutation object that can be used to mint a new NFT token to the connected wallet
 * @beta
 */
export declare function useMintNFT(contract: RequiredParam<Erc721>): import("react-query").UseMutationResult<import("@thirdweb-dev/sdk").TransactionResultWithId<import("@thirdweb-dev/sdk").NFTMetadataOwner>, unknown, NFTMintParams, unknown>;
